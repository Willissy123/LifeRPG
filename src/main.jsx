import React from "react";
import ReactDOM from "react-dom/client";
import ForgettingSpire from "./ForgettingSpire.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ForgettingSpire />
  </React.StrictMode>
);
